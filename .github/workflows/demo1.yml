name: Events Demo 
on: 
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Output event data
        run: echo "{{ toJson(github.event) }}"
      - name: get code
        uses: actions/checkout@v4
      - name: Install dependencies
        run: npm ci
      - name: Test code
        run: npm run test 
      - name: Build code
        run: npm run build
      - name: Deploy project 
        run: echo "Deploying project to production server..."